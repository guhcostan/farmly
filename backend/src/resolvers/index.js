const breedResolver = require("./breedResolver");
const announcementResolver = require("./announcementResolver");

const resolvers = [breedResolver, announcementResolver];

module.exports = resolvers;
